---
term: "pure build"
aliases: ["sandboxed build","impure build","manifest build"]
---

A Flox build mode: pure builds run sandboxed using only git-tracked files (network-isolated on Linux) for reproducibility, versus impure builds that run with the sandbox off.
